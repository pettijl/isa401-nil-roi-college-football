# 01_get_cfbd_data.R
# Legacy filename kept so older instructions do not break.
# This no-key version uses ESPN public scoreboard JSON and Sports Reference scraping instead of CollegeFootballData.

source("scripts/01_get_cfb_no_key_data.R")
